<!DOCTYPE html>
<html>
<head>
    <title>Lista de Productos</title>
</head>
<body>
    <h1>Lista de Productos</h1>
    <ul>
        {{#each products}}
        <li>
            <h3>{{this.title}}</h3> 
            <p>ID: {{this._id}}</p>
            <p>{{this.description}}</p>
            <p>Precio: {{this.price}}</p>
            <p>Stock: {{this.stock}}</p>
            <button onclick="addToCart('{{this._id}}')">Agregar al carrito</button>
        </li>
        {{/each}}
    </ul>

    <div class="pagination">
        {{#if prevLink}}
            <a href="{{prevLink}}">Anterior</a>
        {{/if}}

        {{#if nextLink}}
            <a href="{{nextLink}}">Siguiente</a>
        {{/if}}
    </div>

    <script>
        function addToCart(productId) {
            // Aquí puedes agregar la lógica para enviar una solicitud POST a la ruta `/products/:id/add-to-cart` con el ID del producto seleccionado
            // Puedes utilizar JavaScript o una biblioteca como Axios para realizar la solicitud
            // ...
        }
    </script>
</body>
</html>